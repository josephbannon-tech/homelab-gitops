apiVersion: v2
name: open-webui
description: Open WebUI, the estate chat app for the local LLM tiers (PWA, chat history, Moa RAG). Single replica, SQLite on a PVC.
version: 0.1.0
dependencies:
  - name: open-webui
    version: "16.5.0"
    repository: https://helm.openwebui.com/
