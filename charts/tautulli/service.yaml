apiVersion: v1
kind: Service
metadata:
  name: tautulli
  namespace: monitoring
  labels:
    app: tautulli
spec:
  selector:
    app: tautulli
  ports:
    - name: http
      port: 8181
      targetPort: 8181
